# Agent 配置(已移出)

角色系统(`agent.md`、角色目录、私有技能、角色私有 `mcp.json`、`subagent` 委派)是 **qi-agents
扩展**的功能,不是 core 的。所以本手册已移出 `docs/`:core 只认识运行单元
`{name, prompt, tools}`,**裸启动是单 agent**。

| 想看什么 | 去哪 |
| --- | --- |
| 怎么装、怎么写一个角色、frontmatter 字段、`subagent` 用法 | [README.md](../extensions/qi-agents/README.md) |
| 设计过程与决策记录(为什么这样拆) | [agent-config-design.md](../design/agent-config-design.md) |
| core 与扩展的分界 | [extensions.md](extensions.md) §1 |
